/* Network logs of failed tests can be captured via HAR file

But to capture all the commands executed in test, we can use plugin: cypress-failed-log

Install this plugin via npm:

Now, all logs of failed tests will be printed in JSON stored in cypress/logs/failed.json

*/